# Worker-only local variables go here. Do not place secrets in VITE_* variables.
AUTH_EMAIL_FROM=auth@versus.space
BETTER_AUTH_URL=http://localhost:8787
# Test-only secret. Set production with `wrangler secret put BETTER_AUTH_SECRET`.
BETTER_AUTH_SECRET=local-only-better-auth-secret-change-me
# Test-only Resend key. Set the real key with `wrangler secret put RESEND_API_KEY`.
RESEND_API_KEY=re_local-only-placeholder
SUPERADMIN_EMAILS=worker-admin@example.com
# Test-only Standard Webhooks key. Set the real key with `wrangler secret put DODO_WEBHOOK_KEY`.
DODO_WEBHOOK_KEY=whsec_MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MDE=
